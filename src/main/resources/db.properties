# Connexion PostgreSQL
# Si la base tourne sur une autre machine, remplacer localhost par
# l'adresse IP ou le nom d'hôte du serveur PostgreSQL distant.
# Exemple : db.url=jdbc:postgresql://192.168.1.50:5432/hotel_db
db.url=jdbc:postgresql://localhost:5432/hotel_db
db.user=postgres
db.password=postgres
